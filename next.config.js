/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'tailwindui.com',
            'media.geeksforgeeks.org',
        ]
    }
}

module.exports = nextConfig
